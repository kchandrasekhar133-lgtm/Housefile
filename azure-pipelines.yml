# Azure DevOps Pipeline for House Price Prediction Project

trigger:
  - main

pool:
  vmImage: 'ubuntu-latest'

variables:
  pythonVersion: '3.10'

steps:

# 1. Install Python
- task: UsePythonVersion@0
  displayName: 'Set up Python'
  inputs:
    versionSpec: '$(pythonVersion)'
    addToPath: true

# 2. Upgrade pip
- script: |
    python -m pip install --upgrade pip
  displayName: 'Upgrade pip'

# 3. Install project dependencies
- script: |
    pip install -r requirement.txt
    pip install pytest pytest-cov
  displayName: 'Install Dependencies'

# 4. Run unit tests
- script: |
    pytest test_app_50_cases.py \
      --junitxml=test-results/junit.xml \
      --html=test-results/pytest-report.html \
      --self-contained-html
  displayName: 'Run Unit Tests'

# 5. Publish JUnit test results
- task: PublishTestResults@2
  displayName: 'Publish Unit Test Results'
  condition: succeededOrFailed()
  inputs:
    testResultsFormat: 'JUnit'
    testResultsFiles: 'test-results/junit.xml'
    testRunTitle: 'House Price Prediction Unit Tests'
    failTaskOnFailedTests: true

# 6. Publish HTML test report
- task: PublishBuildArtifacts@1
  displayName: 'Publish Test Report'
  condition: succeededOrFailed()
  inputs:
    PathtoPublish: 'test-results'
    ArtifactName: 'Test-Reports'
    publishLocation: 'Container'